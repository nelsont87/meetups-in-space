<h1> <%= @meetup.name %> <h1>

<ul>
  <li> <%= @meetup.location %> </li>
  <li> <%= @meetup.description %> </li>
  <li> <%= @meetup.creator %> </li>
</ul>

<form action='/meetups/<%= meetup.id %>' method= 'post'>
<button id='join'>Join</button>

<ul>
  <% @users.each do |user| %>
    <li> <%= user.username %> </li>
  <% end %>
</ul>
